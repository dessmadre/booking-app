const dt = new Date();
const now = new Date();
const hour = dt.getHours();
const day = dt.getDate();
const month = dt.getMonth();
const year = dt.getFullYear();

export { dt, hour, day, month, year, now };
